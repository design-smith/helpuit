packages:
  - 'packages/*'
  - 'apps/*'
allowBuilds:
  cloudflared: false # binary is downloaded at runtime via install() on first --tunnel, not at npm-install
  cpu-features: false
  esbuild: true
  protobufjs: false
  ssh2: false
