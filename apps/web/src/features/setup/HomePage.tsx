import { DashboardPage } from '../dashboard/DashboardPage'

/**
 * The console home is the operational dashboard. First-run guidance now lives in
 * the dashboard's dismissible "Getting started" card (video + checklist) rather
 * than a separate forced checklist page.
 */
export function HomePage() {
  return <DashboardPage />
}
